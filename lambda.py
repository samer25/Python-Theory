"""
Lambda operator is used for creating small one time and anonymous function object in python
A lambda function can take any number of arguments, but can only have one expression.

"""

var = lambda x: x + 1
print(var(5))
